// import { chain, spark } from "src/assets";
import { Button, Figure, Heading, Text, Logo } from "src/components";
import { Criteria, GuideLines, Introduction } from "src/sections";

export const Home = () => {
	return (
		<>
			<GuideLines />
		</>
	);
};
